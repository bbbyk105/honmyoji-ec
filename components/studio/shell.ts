/**
 * 管理画面の版面。公開サイトの SHELL（1480px）より狭い。
 *
 * 表を見ながら値を直す画面なので、目が横に走る距離は短いほうがいい。読み物の
 * 版面をそのまま使うと、ステータスの列と操作の列が視野の両端に分かれる。
 */
export const STUDIO_SHELL = "mx-auto w-full max-w-[1180px] px-5 md:px-8";

/** 画面の見出しの下、本文が始まるまでの間。全ページで同じにする。 */
export const STUDIO_HEAD = "border-b border-line pb-7 pt-12 md:pt-16";
